const connect = (a, b, c) => {

    console.log(
        `nilai a : ${a}`
    )

    console.log(
        `nilai b : ${b}`
    )

}

connect(null, 'onLoginUser')
console.log('\n')
connect('mapStateToProps', 'onLoginUser')